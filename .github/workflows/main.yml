name: Try to run SimpleTest class

on: workflow_dispatch # этот иветн значит, что мы его будем запускать в ручном режиме

jobs:
  # Даем любое название
  run_simple_tests:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v3

        - uses: actions/setup-java@v3
          with:
            distribution: 'zulu'
            java-version: '8'



